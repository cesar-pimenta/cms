# Copy this file to .env and update values for your environment
# For development: cp .env.example .env
# For different environments: cp .env.example .env.development / .env.staging / .env.production

# Django Settings
DEBUG=True
SECRET_KEY=change-this-to-a-strong-random-key-in-production
ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0

# Database
DB_ENGINE=django.db.backends.postgresql
DB_NAME=cms_dev
DB_USER=postgres
DB_PASSWORD=your-secure-password-here
DB_HOST=postgres
DB_PORT=5432

# Email (optional - for development use console backend)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password-here

# Security (adjust for your environment)
SECURE_SSL_REDIRECT=False
SECURE_HSTS_SECONDS=0
CSRF_COOKIE_SECURE=False
SESSION_COOKIE_SECURE=False
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=seu-email@gmail.com
EMAIL_HOST_PASSWORD=sua-senha

# Media Files
MEDIA_URL=media/
MEDIA_ROOT=media/

# Timezone
TIME_ZONE=America/Sao_Paulo
